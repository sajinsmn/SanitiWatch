// React and Link imports removed as they're not used in this component

// This component is kept for backward compatibility but Layout now handles navigation
function Navbar() {
  return null; // Layout now handles all navigation
}

export default Navbar;
